import { ContentTargetDirective } from './content-target.directive';

describe('ContentTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentTargetDirective();
    expect(directive).toBeTruthy();
  });
});
