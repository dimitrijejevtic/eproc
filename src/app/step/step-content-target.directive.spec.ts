import { StepContentTargetDirective } from './step-content-target.directive';

describe('StepContentTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new StepContentTargetDirective();
    expect(directive).toBeTruthy();
  });
});
